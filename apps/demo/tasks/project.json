{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/demo/tasks",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/demo/tasks",
        "outputPath": "dist/apps/demo/tasks"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/demo/tasks"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "demo-tasks:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "demo-tasks:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "demo-tasks:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "demo-tasks:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/demo/tasks"],
      "options": {
        "jestConfig": "apps/demo/tasks/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/demo/tasks/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
