# packages-ioc

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build packages-ioc` to build the library.

## Running unit tests

Run `nx test packages-ioc` to execute the unit tests via [Jest](https://jestjs.io).
